# Web Labs MobIDE

## Description
Production environment is Docker on Ubuntu 24.04 ARM64 (glibc), targeting Android 10+ ARM64, using Code-Server as web IDE.

## Toolchains
Production environment is Docker on Ubuntu 24.04 ARM64 (glibc), targeting Android 10+ ARM64, using Code-Server as web IDE.

## Requirements
Production environment is Docker on Ubuntu 24.04 ARM64 (glibc), targeting Android 10+ ARM64, using Code-Server as web IDE.